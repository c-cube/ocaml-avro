
(executable
  (name main)
  (libraries avro-compiler.lib)
  (package avro-compiler)
  (modules main)
  (public_name avro-compiler))


(library
  (name avro_compiler_lib)
  (public_name avro-compiler.lib)
  (libraries yojson avro)
  (modules :standard \ main))
