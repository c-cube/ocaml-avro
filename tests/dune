
(rule
  (alias runtest)
  (deps llist.json)
  (action (run ./bin/parse_print_parse.exe %{deps})))


(rule
  (alias runtest)
  (deps cards.json)
  (action (run ./bin/parse_print_parse.exe %{deps})))


(rule
  (alias runtest)
  (deps avro_file_header.json)
  (action (run ./bin/parse_print_parse.exe %{deps})))


(rule
  (alias runtest)
  (deps records.json)
  (action (run ./bin/parse_print_parse.exe %{deps})))

(rule
  (targets records.ml)
  (deps records.json)
  (action (run ../src/compiler/main.exe %{deps} -o %{targets})))

(executable
  (name records_test)
  (modules records_test records)
  (libraries avro))

(env
  (_ (flags :standard -warn-error -a)))
